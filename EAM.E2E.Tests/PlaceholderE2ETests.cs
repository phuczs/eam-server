using NUnit.Framework;

namespace EAM.E2E.Tests;

/// <summary>
/// Skeleton end-to-end test. Real Playwright browser tests inherit from
/// <c>Microsoft.Playwright.NUnit.PageTest</c> and drive the running SPA + API.
/// This placeholder needs no browser so the project builds and runs out of the box.
/// </summary>
public class PlaceholderE2ETests
{
    [Test]
    public void Skeleton_is_ready_for_playwright_specs()
    {
        Assert.Pass("Add Playwright PageTest-based specs here once the UI exists.");
    }
}
