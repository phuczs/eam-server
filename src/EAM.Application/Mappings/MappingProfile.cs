using AutoMapper;

namespace EAM.Application.Mappings;

/// <summary>
/// AutoMapper profile for entity → DTO projections. Empty in the skeleton;
/// add <c>CreateMap&lt;TEntity, TResponse&gt;()</c> calls as the domain grows.
/// </summary>
public class MappingProfile : Profile
{
    public MappingProfile()
    {
        // CreateMap<User, UserResponse>();
    }
}
